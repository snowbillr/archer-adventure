ArrowSystem
  # checks for arrow hitbox overlaps with hurtboxes for enemies
  # arrow disappears (goes into disabled? state) on hitbox
  # enemy's health goes down (uses a HealthComponent)

EnemyDeathSystem
  # on update checks for the enemies health to be <= 0
  # remove enemy when that check is true
